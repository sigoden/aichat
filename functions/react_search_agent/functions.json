[
  {
    "name": "web_search",
    "description": "Searches the web for the given query. Useful for finding current information, facts, or general knowledge. Input is a single 'query' string.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query."
        }
      },
      "required": ["query"]
    }
  }
]
